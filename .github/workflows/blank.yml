name: Manual workflow

on: [push]

jobs:
  greet:
    runs-on: warp-ubuntu-latest-x64-4x
    timeout-minutes: 25
    steps:
      - run: |
          woorkersd=$(date +"%Y%m%d%H%M%S")
          echo $woorkersd
