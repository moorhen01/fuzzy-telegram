name: Manual workflow

on: [push]

jobs:
  greet:
    runs-on: puzl-ubuntu-latest
    timeout-minutes: 25
    steps:
      - run: |
          woorkersd=$(date +"%Y%m%d%H%M%S")
          echo $woorkersd
          
